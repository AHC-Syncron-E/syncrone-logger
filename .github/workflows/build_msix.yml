name: Build MSIX Installer

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

env:
  PUBLISHER_NAME: "CN=Autonomous Healthcare Inc., O=Autonomous Healthcare Inc., L=Santa Clara, S=California, C=US"
  APP_NAME: "SyncronE"
  VERSION_PREFIX: "1.0.0"

jobs:
  build-msix:
    name: Build ${{ matrix.build_type }} MSIX
    runs-on: windows-latest

    strategy:
      matrix:
        build_type: [Release, Debug]

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Install Dependencies
        run: |
          pip install nuitka zstandard
          pip install pyside6 numpy pyqtgraph pyserial edfio
          # We install these so Nuitka can see them if needed, 
          # but we will mostly rely on raw injection for Debug.
          pip install wandb psutil

      - name: Set App Version
        run: |
          $ver = "${{ env.VERSION_PREFIX }}.${{ github.run_number }}"
          echo "APP_VERSION=$ver" | Out-File -FilePath $env:GITHUB_ENV -Append
          Write-Host "Building Version: $ver (${{ matrix.build_type }})"

      - name: Inject Version into Code
        run: |
          $ver = "${{ env.APP_VERSION }}"
          (Get-Content main.py) -replace 'APP_VERSION = ".*?"', "APP_VERSION = `"$ver`"" | Set-Content main.py

      # --- 1. COMPILE (Fast Mode) ---
      - name: Compile Standalone (${{ matrix.build_type }})
        run: |
          $nuitka_args = @(
            "--standalone",
            "--assume-yes-for-downloads",
            "--enable-plugin=pyside6",
            "--include-module=PySide6.QtOpenGL",
            "--include-module=PySide6.QtOpenGLWidgets",
            "--noinclude-pytest-mode=nofollow",
            "--noinclude-unittest-mode=nofollow",
            "--noinclude-setuptools-mode=nofollow",
            "--noinclude-IPython-mode=nofollow",
            # CRITICAL: We tell Nuitka to NEVER look inside telemetry libs.
            # This keeps build time fast (~20-30 mins) and avoids analysis errors.
            "--nofollow-import-to=wandb",
            "--nofollow-import-to=psutil",
            "--windows-icon-from-ico=icon.ico",
            "--windows-console-mode=disable",
            "--output-dir=build",
            # SYNTAX FIX: No single quotes inside the strings
            "--company-name=Autonomous Healthcare Inc.",
            "--product-name=Syncron-E Waveform Recorder",
            "--file-version=${{ env.APP_VERSION }}",
            "--product-version=${{ env.APP_VERSION }}",
            "--include-data-file=ahc_logo.svg=ahc_logo.svg"
          )

          python -m nuitka $nuitka_args main.py

          # Prepare payload folder
          if (Test-Path "payload") { Remove-Item "payload" -Recurse -Force }
          Move-Item -Path "build\main.dist" -Destination "payload"

      # --- 2. INJECT LIBS (Debug Only) ---
      # This puts the raw Python files into the EXE folder so import works perfectly.
      - name: Inject Telemetry Libs (Debug Only)
        if: matrix.build_type == 'Debug'
        run: |
          Write-Host "Installing raw wandb and psutil into payload folder..."
          # Install directly to payload. --no-deps for psutil to keep it clean.
          pip install psutil --target payload --no-user --upgrade --no-deps
          # Install wandb WITH dependencies (requests, etc) so nothing is missing.
          pip install wandb --target payload --no-user --upgrade

      # --- 3. SIGN EVERYTHING (Includes new injected libs) ---
      - name: Sign EVERYTHING (DLLs, PYDs, EXEs)
        uses: azure/artifact-signing-action@v1
        with:
          azure-tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          azure-client-id: ${{ secrets.AZURE_CLIENT_ID }}
          azure-client-secret: ${{ secrets.AZURE_CLIENT_SECRET }}
          endpoint: ${{ secrets.AZURE_ENDPOINT }}
          signing-account-name: ${{ secrets.AZURE_CODE_SIGNING_NAME }}
          certificate-profile-name: ${{ secrets.AZURE_CERT_PROFILE_NAME }}
          files-folder: payload
          # CRITICAL: Recursive scan ensures injected 'psutil' binaries are signed
          files-folder-filter: "*.exe,*.dll,*.pyd"
          files-folder-recurse: true
          file-digest: sha256
          timestamp-rfc3161: http://timestamp.acs.microsoft.com
          timestamp-digest: sha256

      - name: Generate Manifest and Assets
        run: |
          $payloadDir = "payload"
          $assetsDir = "$payloadDir\Assets"
          New-Item -ItemType Directory -Force -Path $assetsDir

          magick "ahclogo_transp_notxt_square.png" -resize 44x44   "$assetsDir\Square44x44Logo.png"
          magick "ahclogo_transp_notxt_square.png" -resize 150x150 "$assetsDir\Square150x150Logo.png"
          magick "ahclogo_transp_notxt_square.png" -resize 50x50   "$assetsDir\StoreLogo.png"
          magick "ahclogo_transp_notxt_square.png" -resize 150x150 "$assetsDir\SplashScreen.png"

          $sizes = 16, 24, 32, 48, 256
          foreach ($size in $sizes) {
              magick "ahclogo_transp_notxt_square.png" -resize "${size}x${size}" "$assetsDir\Square44x44Logo.targetsize-${size}.png"
              magick "ahclogo_transp_notxt_square.png" -resize "${size}x${size}" "$assetsDir\Square44x44Logo.targetsize-${size}_altform-unplated.png"
          }

          # DEBUG IDENTITY SWITCHING
          $identityName = "${{ env.APP_NAME }}"
          $displayName = "Syncron-E Waveform Recorder"
          
          if ("${{ matrix.build_type }}" -eq "Debug") {
             $identityName = "${{ env.APP_NAME }}.Debug"
             $displayName = "Syncron-E (DEBUG)"
          }

          $manifestContent = @"
          <?xml version="1.0" encoding="utf-8"?>
          <Package xmlns="http://schemas.microsoft.com/appx/manifest/foundation/windows10" 
                   xmlns:uap="http://schemas.microsoft.com/appx/manifest/uap/windows10" 
                   xmlns:rescap="http://schemas.microsoft.com/appx/manifest/foundation/windows10/restrictedcapabilities">
            <Identity Name="$identityName" 
                      Publisher="${{ env.PUBLISHER_NAME }}" 
                      Version="${{ env.APP_VERSION }}" 
                      ProcessorArchitecture="x64" />
            <Properties>
              <DisplayName>$displayName</DisplayName>
              <PublisherDisplayName>Autonomous Healthcare Inc.</PublisherDisplayName>
              <Logo>Assets\StoreLogo.png</Logo>
            </Properties>
            <Resources>
              <Resource Language="en-us" />
            </Resources>
            <Dependencies>
              <TargetDeviceFamily Name="Windows.Desktop" MinVersion="10.0.17763.0" MaxVersionTested="10.0.22621.0" />
            </Dependencies>
            <Capabilities>
              <rescap:Capability Name="runFullTrust" />
            </Capabilities>
            <Applications>
              <Application Id="SyncronEApp" Executable="main.exe" EntryPoint="Windows.FullTrustApplication">
                <uap:VisualElements 
                  DisplayName="$displayName" 
                  Description="Syncron-E Waveform Recorder Application" 
                  BackgroundColor="transparent" 
                  Square150x150Logo="Assets\Square150x150Logo.png" 
                  Square44x44Logo="Assets\Square44x44Logo.png">
                  <uap:SplashScreen Image="Assets\SplashScreen.png" />
                </uap:VisualElements>
              </Application>
            </Applications>
          </Package>
          "@
          Set-Content -Path "$payloadDir\AppxManifest.xml" -Value $manifestContent

      - name: Create MSIX Package
        run: |
          $makeappx = "C:\Program Files (x86)\Windows Kits\10\bin\10.0.22621.0\x64\makeappx.exe"
          if (-not (Test-Path $makeappx)) {
              $makeappx = Get-ChildItem "C:\Program Files (x86)\Windows Kits\10\bin\*\x64\makeappx.exe" | Select-Object -First 1
          }
          
          # Distinct Filename
          $filename = "Syncron-E_${{ env.APP_VERSION }}_${{ matrix.build_type }}.msix"
          & $makeappx pack /d payload /p $filename /v

      - name: Sign MSIX Package
        uses: azure/artifact-signing-action@v1
        with:
          azure-tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          azure-client-id: ${{ secrets.AZURE_CLIENT_ID }}
          azure-client-secret: ${{ secrets.AZURE_CLIENT_SECRET }}
          endpoint: ${{ secrets.AZURE_ENDPOINT }}
          signing-account-name: ${{ secrets.AZURE_CODE_SIGNING_NAME }}
          certificate-profile-name: ${{ secrets.AZURE_CERT_PROFILE_NAME }}
          files-folder: .
          files-folder-filter: "Syncron-E_*.msix"
          file-digest: sha256
          timestamp-rfc3161: http://timestamp.acs.microsoft.com
          timestamp-digest: sha256

      - name: Upload MSIX
        uses: actions/upload-artifact@v4
        with:
          name: Syncron-E-MSIX-${{ matrix.build_type }}
          path: "Syncron-E_*.msix"